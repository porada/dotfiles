#!/bin/sh

# Enable strict mode
set -eu

# Log a given number of last commits
if test $# -gt 0; then
  eval "git --no-pager log $(git log-format)" | grep -i -- "$*" || true
else
  eval "git --no-pager log $(git log-format --refs) -n $(git default-commit-count)"
fi
