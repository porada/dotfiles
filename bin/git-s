#!/bin/sh

# shellcheck source=bin/domlib
. "$(dirname "$(realpath "$0")")/domlib"

# Show the current `git status` including skipped files
git status --short "$@" || exit $?
__domfiles_bin git-skipped
