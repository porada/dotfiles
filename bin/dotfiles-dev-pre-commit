#!/bin/sh

# shellcheck source=bin/dotlib
. "$(dirname "$(realpath "$0")")/dotlib"

# Ensure all dependencies are up to date
__dotfiles_pnpm_install

# Lint staged files
__dotfiles_pnpm exec lint-staged --relative
