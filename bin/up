#!/bin/sh

# Enable strict mode
set -eu

# Don’t let the system go to sleep
if [ "$(pgrep caffeinate | wc -l)" -gt 0 ]; then
  echo "The system is already caffeinated."
  exit 1
fi

caffeinate -im "$@"
