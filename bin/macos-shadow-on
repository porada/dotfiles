#!/bin/sh

# shellcheck source=bin/dotlib
. "$(dirname "$(realpath "$0")")/dotlib"

# Enable window shadows when taking screenshots
defaults write com.apple.screencapture disable-shadow -bool false
killall SystemUIServer || true
