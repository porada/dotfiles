#!/bin/sh

# shellcheck source=bin/domlib
. "$(dirname "$(realpath "$0")")/domlib"

# Enable window shadows when taking screenshots
__ defaults write com.apple.screencapture disable-shadow -bool false
__ killall SystemUIServer || true
