#!/bin/sh

# shellcheck source=bin/dotlib
. "$(dirname "$(realpath "$0")")/dotlib"

# Show diff without clearing the terminal window unless it’s necessary
git diff-files --quiet || clear
git diff "$@"
