#!/bin/sh

# Update Homebrew and installed apps, then check if everything’s fine
brew update && brew upgrade && brew cleanup; brew doctor && brew cleanup --force

# Update globally installed Node packages
npm upgrade --global yarn && yarn global upgrade

# Update RubyGems and installed gems
gem update --system && gem uninstall --all --executables --ignore-dependencies && install-dependencies ruby
