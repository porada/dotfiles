#!/bin/sh

# shellcheck source=bin/domlib
. "$(dirname "$(realpath "$0")")/domlib"

# Disable window shadows when taking screenshots
__ defaults write com.apple.screencapture disable-shadow -bool true
__ killall SystemUIServer || true
