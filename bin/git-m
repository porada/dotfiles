#!/bin/sh

# shellcheck source=bin/dotlib
. "$(dirname "$(realpath "$0")")/dotlib"

# Go to the main branch, whichever it is
git checkout main 2> /dev/null \
  || git checkout trunk 2> /dev/null \
  || git checkout master
