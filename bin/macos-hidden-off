#!/bin/sh

# shellcheck source=bin/dotlib
. "$(dirname "$(realpath "$0")")/dotlib"

# Hide hidden files in Finder
__ defaults write com.apple.finder AppleShowAllFiles -bool false
__ killall Finder || true
