#!/bin/sh

# Enable strict mode
set -eu

# Show files ignored by `git skip`
print_skipped() {
  printf "\033[1;33m S\033[0m %s\n" "${1#h }"
}

git ls-files -v | grep "^h" | while read -r line; do
  print_skipped "$line"
done
