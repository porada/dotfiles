#!/bin/sh

# Enable strict mode
set -eu

# Set up all dotfiles
DOTFILES_BIN_PATH="$(dirname "$(realpath "$0")")"

"$DOTFILES_BIN_PATH/dotfiles-init-migrate"
"$DOTFILES_BIN_PATH/dotfiles-init-setup"
"$DOTFILES_BIN_PATH/dotfiles-init-install"
"$DOTFILES_BIN_PATH/dotfiles-macos"
