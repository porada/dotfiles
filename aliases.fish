# Quick navigation
abbr p "cd ~/Projects"

# Shorten the frequently used commands
abbr c "code"
abbr g "git"
abbr o "open"
abbr v "vim"

# Show hidden files by default when using `ls`
alias ls="ls -A"
